{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"pyth-hermes","text":"<p>A typed Python client for the Pyth Network Hermes price-oracle API. Sync and async clients, Pydantic v2 models, Server-Sent-Events price streaming with auto-reconnect, and a <code>Decimal</code> price helper.</p> <ul> <li>Sync (<code>HermesClient</code>) and async (<code>AsyncHermesClient</code>) APIs over <code>httpx</code></li> <li>SSE streaming with reconnect + backoff</li> <li>Graceful 429 rate-limit handling (retries that respect the 60s window)</li> <li>Configurable <code>base_url</code> (production, beta, or paid providers) and optional API key from day one</li> <li><code>mypy --strict</code> clean, fully type-hinted, ships <code>py.typed</code></li> </ul>"},{"location":"#install","title":"Install","text":"<pre><code>pip install pyth-hermes\n# with the optional pandas helper:\npip install \"pyth-hermes[pandas]\"\n</code></pre>"},{"location":"#quickstart-btcusd-price-in-under-5-lines","title":"Quickstart \u2014 BTC/USD price in under 5 lines","text":"<pre><code>from pyth_hermes import HermesClient\n\nclient = HermesClient()\nfeed_id = client.get_feed_id(\"Crypto.BTC/USD\")   # exact-symbol lookup\nprint(client.get_price_decimal(feed_id))         # -&gt; Decimal(\"63952.82...\")\n</code></pre>"},{"location":"#async-streaming","title":"Async + streaming","text":"<pre><code>import asyncio\nfrom pyth_hermes import AsyncHermesClient\n\nBTC = \"e62df6c8b4a85fe1a67db44dc12de5db330f7ac66b72dc658afedf0f4a415b43\"\n\nasync def main():\n    async with AsyncHermesClient() as client:\n        async for update in client.stream_prices([BTC]):\n            print(update.parsed[0].to_decimal())\n\nasyncio.run(main())\n</code></pre>"},{"location":"#historical-price","title":"Historical price","text":"<pre><code>resp = client.get_price_at(1718900000, [BTC])   # unix timestamp\nprint(resp.parsed[0].to_decimal())\n</code></pre>"},{"location":"#pandas","title":"pandas","text":"<pre><code>from pyth_hermes.pandas import updates_to_dataframe\ndf = updates_to_dataframe([client.get_latest_price([BTC])])\n</code></pre>"},{"location":"#prices-and-exponents","title":"Prices and exponents","text":"<p>Pyth returns integer prices plus an exponent. The real value is <code>price * 10**expo</code>, computed exactly as a <code>Decimal</code>:</p> <pre><code>from pyth_hermes import price_to_decimal\nprice_to_decimal(6395282153102, -8)   # Decimal(\"63952.82153102\")\n</code></pre> <p><code>RpcPrice.to_decimal()</code> and <code>ParsedPriceUpdate.to_decimal()</code> are convenience wrappers.</p>"},{"location":"#finding-feed-ids-use-the-exact-symbol","title":"Finding feed ids \u2014 use the EXACT symbol","text":"<p><code>/v2/price_feeds?query=btc</code> returns deprecated / variant feeds (e.g. <code>MBTC</code>, <code>XBTC</code>) before the canonical one and matches substrings. <code>get_feed_id()</code> therefore matches on exact <code>attributes.symbol</code>:</p> <pre><code>client.get_feed_id(\"Crypto.BTC/USD\")\n# -&gt; \"e62df6c8b4a85fe1a67db44dc12de5db330f7ac66b72dc658afedf0f4a415b43\"\n</code></pre>"},{"location":"#authentication-changes-2026-07-31","title":"\ud83d\udd34 Authentication (changes 2026-07-31)","text":"<p>Today the public endpoint needs no API key. From 2026-07-31 an API key becomes mandatory. This client accepts one from day one \u2014 pass it now to be ready:</p> <pre><code>client = HermesClient(api_key=\"YOUR_KEY\")  # default: Authorization: Bearer YOUR_KEY\n</code></pre> <p>The exact header is not finalized publicly, so both the header name and scheme are configurable:</p> <pre><code>HermesClient(api_key=\"KEY\", api_key_header=\"X-Api-Key\", api_key_scheme=\"\")  # -&gt; X-Api-Key: KEY\n</code></pre>"},{"location":"#endpoints-base-urls","title":"Endpoints / base URLs","text":"<pre><code>from pyth_hermes import HermesClient\n\nHermesClient()                                           # production: https://hermes.pyth.network\nHermesClient(base_url=\"https://hermes-beta.pyth.network\")  # beta\nHermesClient(base_url=\"https://your-paid-provider.example\")  # Triton / P2P / extrnode / Liquify\n</code></pre> <p>You may also inject your own preconfigured <code>httpx.Client</code> / <code>httpx.AsyncClient</code> via <code>client=...</code> (e.g. for custom transports, proxies, or connection pools). In that case the request host is taken from your client, so set <code>base_url</code> on the client itself \u2014 passing both <code>base_url=</code> and <code>client=</code> raises a <code>UserWarning</code> because the constructor's <code>base_url</code> would be a no-op. The <code>api_key</code> is still applied per-request, so it works with an injected client.</p> <pre><code>import httpx\nfrom pyth_hermes import HermesClient\n\nhttp = httpx.Client(base_url=\"https://your-paid-provider.example\", proxy=\"http://localhost:8080\")\nclient = HermesClient(api_key=\"KEY\", client=http)  # base_url comes from `http`\n</code></pre>"},{"location":"#rate-limits","title":"Rate limits","text":"<p>The public endpoint allows 10 requests / 10 seconds per IP. Exceeding it returns HTTP 429 for the next 60 seconds. The client retries 429 and 5xx responses with exponential backoff + jitter, honoring any <code>Retry-After</code> header and never exceeding the 60s rate-limit window per delay. Tune via <code>max_retries</code>, <code>backoff_base</code>, <code>backoff_cap</code>.</p>"},{"location":"#not-implemented","title":"Not implemented","text":"<p>The TWAP endpoints (<code>/v2/updates/twap/...</code>) are intentionally omitted \u2014 the API returns HTTP 400 \"deprecated and no longer available\".</p>"},{"location":"#development","title":"Development","text":"<pre><code>pip install -e \".[dev]\"\npytest                 # unit tests (no network)\npytest -m integration  # live smoke tests against production\nmypy\nruff check .\n</code></pre>"},{"location":"#license","title":"License","text":"<p>MIT</p>"},{"location":"api/","title":"API reference","text":""},{"location":"api/#pyth_hermes","title":"<code>pyth_hermes</code>","text":"<p>pyth-hermes: a Python client for the Pyth Network Hermes price-oracle API.</p>"},{"location":"api/#pyth_hermes.AsyncHermesClient","title":"<code>AsyncHermesClient</code>","text":"<p>Asynchronous client for the Pyth Network Hermes API.</p> <p>Adds :meth:<code>stream_prices</code>, an async iterator over the SSE price stream with automatic reconnect + backoff. Mirrors :class:<code>~pyth_hermes.HermesClient</code> for the request/response endpoints, and is an async context manager::</p> <pre><code>async with AsyncHermesClient() as client:\n    price = await client.get_price_decimal(feed_id)\n</code></pre> <p>As with the sync client, transient failures (429/5xx) are retried with exponential backoff that honors <code>Retry-After</code> and the 60-second rate-limit window.</p>"},{"location":"api/#pyth_hermes.AsyncHermesClient.base_url","title":"<code>base_url</code>  <code>property</code>","text":"<p>The normalized API base URL in use (no trailing slash).</p>"},{"location":"api/#pyth_hermes.AsyncHermesClient.__init__","title":"<code>__init__(base_url=_BASE_URL_UNSET, *, api_key=None, api_key_header='Authorization', api_key_scheme='Bearer', timeout=30.0, max_retries=3, backoff_base=1.0, backoff_cap=60.0, client=None)</code>","text":"<p>Construct an asynchronous Hermes client.</p> <p>Parameters:</p> Name Type Description Default <code>base_url</code> <code>str</code> <p>API base URL. Defaults to production (<code>https://hermes.pyth.network</code>). Set to the beta host or a paid provider's URL as needed. Ignored (with a <code>UserWarning</code>) when <code>client</code> is supplied.</p> <code>_BASE_URL_UNSET</code> <code>api_key</code> <code>Optional[str]</code> <p>Optional API key. Optional today; mandatory for all callers from 2026-07-31. Sent on every request as a header.</p> <code>None</code> <code>api_key_header</code> <code>str</code> <p>Header name to carry the API key. Defaults to <code>\"Authorization\"</code>.</p> <code>'Authorization'</code> <code>api_key_scheme</code> <code>str</code> <p>Auth scheme prefix for the header value. Defaults to <code>\"Bearer\"</code>. Pass an empty string to send the raw key.</p> <code>'Bearer'</code> <code>timeout</code> <code>float</code> <p>Per-request timeout in seconds.</p> <code>30.0</code> <code>max_retries</code> <code>int</code> <p>Maximum retry attempts for transient errors, in addition to the initial attempt.</p> <code>3</code> <code>backoff_base</code> <code>float</code> <p>Base seconds for exponential backoff between retries.</p> <code>1.0</code> <code>backoff_cap</code> <code>float</code> <p>Maximum seconds any single backoff delay may reach.</p> <code>60.0</code> <code>client</code> <code>Optional[AsyncClient]</code> <p>Optional preconfigured :class:<code>httpx.AsyncClient</code>. When given, set <code>base_url</code> on the client itself; the constructor's <code>base_url</code> is then ignored.</p> <code>None</code> <p>Raises:</p> Type Description <code>UserWarning</code> <p>If both an explicit <code>base_url</code> and <code>client</code> are passed (the former cannot take effect).</p>"},{"location":"api/#pyth_hermes.AsyncHermesClient.aclose","title":"<code>aclose()</code>  <code>async</code>","text":"<p>Close the underlying async HTTP connection pool.</p> <p>Called automatically when the client is used as an async context manager.</p>"},{"location":"api/#pyth_hermes.AsyncHermesClient.list_price_feeds","title":"<code>list_price_feeds(*, query=None, asset_type=None)</code>  <code>async</code>","text":"<p>List the price-feed catalog, optionally filtered.</p> <p>Async counterpart of :meth:<code>~pyth_hermes.HermesClient.list_price_feeds</code>. Calls <code>GET /v2/price_feeds</code>; the search is fuzzy, so prefer :meth:<code>get_feed_id</code> to resolve a single canonical feed.</p> <p>Parameters:</p> Name Type Description Default <code>query</code> <code>Optional[str]</code> <p>Case-insensitive substring to filter feeds by symbol.</p> <code>None</code> <code>asset_type</code> <code>Optional[str]</code> <p>Asset class filter, e.g. <code>\"crypto\"</code>, <code>\"fx\"</code>, <code>\"equity\"</code>, <code>\"metal\"</code>.</p> <code>None</code> <p>Returns:</p> Type Description <code>list[PriceFeed]</code> <p>A list of :class:<code>~pyth_hermes.models.PriceFeed</code> catalog entries.</p> <p>Raises:</p> Type Description <code>HTTPStatusError</code> <p>If the API returns a non-2xx status after retries are exhausted.</p>"},{"location":"api/#pyth_hermes.AsyncHermesClient.get_feed_id","title":"<code>get_feed_id(symbol)</code>  <code>async</code>","text":"<p>Resolve a canonical feed id by EXACT <code>attributes.symbol</code> match.</p> <p>Async counterpart of :meth:<code>~pyth_hermes.HermesClient.get_feed_id</code>. The catalog search returns deprecated/variant feeds (<code>MBTC</code>, <code>XBTC</code>, ...) and matches substrings, so this filters on exact symbol equality.</p> <p>Parameters:</p> Name Type Description Default <code>symbol</code> <code>str</code> <p>The canonical symbol to match exactly, e.g. <code>\"Crypto.BTC/USD\"</code>.</p> required <p>Returns:</p> Type Description <code>Optional[str]</code> <p>The 32-byte hex feed id, or <code>None</code> if no feed matches exactly.</p> <p>Raises:</p> Type Description <code>HTTPStatusError</code> <p>If the underlying catalog request fails after retries are exhausted.</p>"},{"location":"api/#pyth_hermes.AsyncHermesClient.get_latest_price","title":"<code>get_latest_price(ids, *, parsed=True, encoding='hex')</code>  <code>async</code>","text":"<p>Fetch the latest price update for one or more feed ids.</p> <p>Async counterpart of :meth:<code>~pyth_hermes.HermesClient.get_latest_price</code>. Calls <code>GET /v2/updates/price/latest</code>.</p> <p>Parameters:</p> Name Type Description Default <code>ids</code> <code>list[str]</code> <p>One or more 32-byte hex feed ids (sent as repeatable <code>ids[]</code>).</p> required <code>parsed</code> <code>bool</code> <p>Whether to include decoded per-feed updates. When <code>False</code>, only the binary payload is returned and <code>parsed</code> is <code>None</code>.</p> <code>True</code> <code>encoding</code> <code>str</code> <p>Encoding for the binary payload: <code>\"hex\"</code> or <code>\"base64\"</code>.</p> <code>'hex'</code> <p>Returns:</p> Name Type Description <code>A</code> <code>PriceUpdateResponse</code> <p>class:<code>~pyth_hermes.models.PriceUpdateResponse</code>.</p> <p>Raises:</p> Type Description <code>HTTPStatusError</code> <p>If the API returns a non-2xx status after retries are exhausted.</p>"},{"location":"api/#pyth_hermes.AsyncHermesClient.get_price_at","title":"<code>get_price_at(publish_time, ids, *, parsed=True, encoding='hex')</code>  <code>async</code>","text":"<p>Fetch a historical price update at or just after a timestamp.</p> <p>Async counterpart of :meth:<code>~pyth_hermes.HermesClient.get_price_at</code>. Calls <code>GET /v2/updates/price/{publish_time}</code>; the API returns the first update with a publish time at or after the requested timestamp.</p> <p>Parameters:</p> Name Type Description Default <code>publish_time</code> <code>int</code> <p>Unix timestamp (seconds) to look up.</p> required <code>ids</code> <code>list[str]</code> <p>One or more 32-byte hex feed ids (sent as repeatable <code>ids[]</code>).</p> required <code>parsed</code> <code>bool</code> <p>Whether to include decoded per-feed updates.</p> <code>True</code> <code>encoding</code> <code>str</code> <p>Encoding for the binary payload: <code>\"hex\"</code> or <code>\"base64\"</code>.</p> <code>'hex'</code> <p>Returns:</p> Name Type Description <code>A</code> <code>PriceUpdateResponse</code> <p>class:<code>~pyth_hermes.models.PriceUpdateResponse</code> for that instant.</p> <p>Raises:</p> Type Description <code>HTTPStatusError</code> <p>If the API returns a non-2xx status after retries are exhausted.</p>"},{"location":"api/#pyth_hermes.AsyncHermesClient.get_price_decimal","title":"<code>get_price_decimal(feed_id)</code>  <code>async</code>","text":"<p>Return the latest human-readable spot price for one feed id.</p> <p>Async counterpart of :meth:<code>~pyth_hermes.HermesClient.get_price_decimal</code>.</p> <p>Parameters:</p> Name Type Description Default <code>feed_id</code> <code>str</code> <p>A single 32-byte hex feed id.</p> required <p>Returns:</p> Type Description <code>Decimal</code> <p>The latest spot price as an exact :class:<code>~decimal.Decimal</code>.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the API returns no parsed price for <code>feed_id</code>.</p> <code>HTTPStatusError</code> <p>If the API returns a non-2xx status after retries are exhausted.</p>"},{"location":"api/#pyth_hermes.AsyncHermesClient.stream_prices","title":"<code>stream_prices(ids, *, parsed=True, reconnect=True)</code>  <code>async</code>","text":"<p>Stream live price updates over Server-Sent Events (SSE).</p> <p>Opens <code>GET /v2/updates/price/stream</code> and yields one :class:<code>~pyth_hermes.models.PriceUpdateResponse</code> per event. With <code>reconnect=True</code> (the default) the stream transparently re-opens after a disconnect or transient error, applying exponential backoff between attempts (the backoff counter resets after each successful connect). With <code>reconnect=False</code> the iterator stops when the server closes the stream and re-raises transport/HTTP errors.</p> <p>Parameters:</p> Name Type Description Default <code>ids</code> <code>list[str]</code> <p>One or more 32-byte hex feed ids to subscribe to (sent as repeatable <code>ids[]</code>).</p> required <code>parsed</code> <code>bool</code> <p>Whether each event should include decoded per-feed updates.</p> <code>True</code> <code>reconnect</code> <code>bool</code> <p>Whether to automatically reconnect with backoff after a disconnect or transient error. Defaults to <code>True</code>.</p> <code>True</code> <p>Yields:</p> Name Type Description <code>A</code> <code>AsyncIterator[PriceUpdateResponse]</code> <p>class:<code>~pyth_hermes.models.PriceUpdateResponse</code> for each SSE event.</p> <p>Raises:</p> Type Description <code>HTTPStatusError</code> <p>On a non-2xx stream response when <code>reconnect=False</code>.</p> <code>TransportError</code> <p>On a connection failure when <code>reconnect=False</code>.</p> Example <p>btc = \"e62df6c8b4a85fe1a67db44dc12de5db330f7ac66b72dc658afedf0f4a415b43\" async with AsyncHermesClient() as client: ...     async for update in client.stream_prices([btc]): ...         if update.parsed: ...             print(update.parsed[0].to_decimal()) ...             break  # stop after the first tick</p>"},{"location":"api/#pyth_hermes.HermesClient","title":"<code>HermesClient</code>","text":"<p>Synchronous client for the Pyth Network Hermes API.</p> <p>Get a BTC/USD price in a few lines::</p> <pre><code>from pyth_hermes import HermesClient\nclient = HermesClient()\nfeed_id = client.get_feed_id(\"Crypto.BTC/USD\")\nprint(client.get_price_decimal(feed_id))\n</code></pre> <p>An <code>api_key</code> and custom <code>base_url</code> may be supplied for paid providers and for the mandatory-key requirement landing 2026-07-31. The client is also a context manager, which closes the underlying HTTP connection on exit::</p> <pre><code>with HermesClient() as client:\n    ...\n</code></pre> <p>Transient failures (HTTP 429 and 5xx) are retried automatically with exponential backoff that honors any <code>Retry-After</code> header and respects the 60-second rate-limit window.</p>"},{"location":"api/#pyth_hermes.HermesClient.base_url","title":"<code>base_url</code>  <code>property</code>","text":"<p>The normalized API base URL in use (no trailing slash).</p>"},{"location":"api/#pyth_hermes.HermesClient.__init__","title":"<code>__init__(base_url=_BASE_URL_UNSET, *, api_key=None, api_key_header='Authorization', api_key_scheme='Bearer', timeout=30.0, max_retries=3, backoff_base=1.0, backoff_cap=60.0, client=None)</code>","text":"<p>Construct a synchronous Hermes client.</p> <p>Parameters:</p> Name Type Description Default <code>base_url</code> <code>str</code> <p>API base URL. Defaults to production (<code>https://hermes.pyth.network</code>). Set to the beta host or a paid provider's URL as needed. Ignored (with a <code>UserWarning</code>) when <code>client</code> is supplied, since the request host then comes from the injected client.</p> <code>_BASE_URL_UNSET</code> <code>api_key</code> <code>Optional[str]</code> <p>Optional API key. Optional today; mandatory for all callers from 2026-07-31. Sent on every request as a header.</p> <code>None</code> <code>api_key_header</code> <code>str</code> <p>Header name to carry the API key. Defaults to <code>\"Authorization\"</code>.</p> <code>'Authorization'</code> <code>api_key_scheme</code> <code>str</code> <p>Auth scheme prefix for the header value. Defaults to <code>\"Bearer\"</code> (producing <code>Authorization: Bearer &lt;key&gt;</code>). Pass an empty string to send the raw key with no prefix.</p> <code>'Bearer'</code> <code>timeout</code> <code>float</code> <p>Per-request timeout in seconds.</p> <code>30.0</code> <code>max_retries</code> <code>int</code> <p>Maximum retry attempts for transient errors (429/5xx and transport errors), in addition to the initial attempt.</p> <code>3</code> <code>backoff_base</code> <code>float</code> <p>Base seconds for exponential backoff between retries.</p> <code>1.0</code> <code>backoff_cap</code> <code>float</code> <p>Maximum seconds any single backoff delay may reach.</p> <code>60.0</code> <code>client</code> <code>Optional[Client]</code> <p>Optional preconfigured :class:<code>httpx.Client</code> (custom transport, proxy, pool, ...). When given, set <code>base_url</code> on the client itself; the constructor's <code>base_url</code> is then ignored.</p> <code>None</code> <p>Raises:</p> Type Description <code>UserWarning</code> <p>If both an explicit <code>base_url</code> and <code>client</code> are passed (the former cannot take effect).</p>"},{"location":"api/#pyth_hermes.HermesClient.close","title":"<code>close()</code>","text":"<p>Close the underlying HTTP connection pool.</p> <p>Called automatically when the client is used as a context manager.</p>"},{"location":"api/#pyth_hermes.HermesClient.list_price_feeds","title":"<code>list_price_feeds(*, query=None, asset_type=None)</code>","text":"<p>List the price-feed catalog, optionally filtered.</p> <p>Calls <code>GET /v2/price_feeds</code>. Note the search is fuzzy and returns deprecated/variant feeds; for resolving a single canonical feed prefer :meth:<code>get_feed_id</code>, which matches on exact symbol.</p> <p>Parameters:</p> Name Type Description Default <code>query</code> <code>Optional[str]</code> <p>Case-insensitive substring to filter feeds by symbol.</p> <code>None</code> <code>asset_type</code> <code>Optional[str]</code> <p>Asset class filter, e.g. <code>\"crypto\"</code>, <code>\"fx\"</code>, <code>\"equity\"</code>, <code>\"metal\"</code>.</p> <code>None</code> <p>Returns:</p> Type Description <code>list[PriceFeed]</code> <p>A list of :class:<code>~pyth_hermes.models.PriceFeed</code> catalog entries.</p> <p>Raises:</p> Type Description <code>HTTPStatusError</code> <p>If the API returns a non-2xx status after retries are exhausted.</p>"},{"location":"api/#pyth_hermes.HermesClient.get_feed_id","title":"<code>get_feed_id(symbol)</code>","text":"<p>Resolve a canonical feed id by EXACT <code>attributes.symbol</code> match.</p> <p><code>GET /v2/price_feeds?query=...</code> returns deprecated/variant feeds (<code>MBTC</code>, <code>XBTC</code>, ...) first and matches substrings, so this filters on exact symbol equality to avoid picking the wrong feed.</p> <p>Parameters:</p> Name Type Description Default <code>symbol</code> <code>str</code> <p>The canonical symbol to match exactly, e.g. <code>\"Crypto.BTC/USD\"</code>.</p> required <p>Returns:</p> Type Description <code>Optional[str]</code> <p>The 32-byte hex feed id, or <code>None</code> if no feed matches exactly.</p> <p>Raises:</p> Type Description <code>HTTPStatusError</code> <p>If the underlying catalog request fails after retries are exhausted.</p> Example <p>client.get_feed_id(\"Crypto.BTC/USD\") 'e62df6c8b4a85fe1a67db44dc12de5db330f7ac66b72dc658afedf0f4a415b43'</p>"},{"location":"api/#pyth_hermes.HermesClient.get_latest_price","title":"<code>get_latest_price(ids, *, parsed=True, encoding='hex')</code>","text":"<p>Fetch the latest price update for one or more feed ids.</p> <p>Calls <code>GET /v2/updates/price/latest</code>.</p> <p>Parameters:</p> Name Type Description Default <code>ids</code> <code>list[str]</code> <p>One or more 32-byte hex feed ids (sent as repeatable <code>ids[]</code>).</p> required <code>parsed</code> <code>bool</code> <p>Whether to include decoded per-feed updates. When <code>False</code>, only the binary payload is returned and <code>parsed</code> is <code>None</code>.</p> <code>True</code> <code>encoding</code> <code>str</code> <p>Encoding for the binary payload: <code>\"hex\"</code> or <code>\"base64\"</code>.</p> <code>'hex'</code> <p>Returns:</p> Name Type Description <code>A</code> <code>PriceUpdateResponse</code> <p>class:<code>~pyth_hermes.models.PriceUpdateResponse</code> with the binary</p> <code>PriceUpdateResponse</code> <p>payload and (when <code>parsed</code>) the decoded updates.</p> <p>Raises:</p> Type Description <code>HTTPStatusError</code> <p>If the API returns a non-2xx status after retries are exhausted.</p> Example <p>btc = \"e62df6c8b4a85fe1a67db44dc12de5db330f7ac66b72dc658afedf0f4a415b43\" resp = client.get_latest_price([btc]) resp.parsed[0].to_decimal() Decimal('63952.82153102')</p>"},{"location":"api/#pyth_hermes.HermesClient.get_price_at","title":"<code>get_price_at(publish_time, ids, *, parsed=True, encoding='hex')</code>","text":"<p>Fetch a historical price update at or just after a timestamp.</p> <p>Calls <code>GET /v2/updates/price/{publish_time}</code>; the API returns the first update with a publish time greater than or equal to the requested timestamp.</p> <p>Parameters:</p> Name Type Description Default <code>publish_time</code> <code>int</code> <p>Unix timestamp (seconds) to look up.</p> required <code>ids</code> <code>list[str]</code> <p>One or more 32-byte hex feed ids (sent as repeatable <code>ids[]</code>).</p> required <code>parsed</code> <code>bool</code> <p>Whether to include decoded per-feed updates.</p> <code>True</code> <code>encoding</code> <code>str</code> <p>Encoding for the binary payload: <code>\"hex\"</code> or <code>\"base64\"</code>.</p> <code>'hex'</code> <p>Returns:</p> Name Type Description <code>A</code> <code>PriceUpdateResponse</code> <p>class:<code>~pyth_hermes.models.PriceUpdateResponse</code> for that instant.</p> <p>Raises:</p> Type Description <code>HTTPStatusError</code> <p>If the API returns a non-2xx status after retries are exhausted.</p>"},{"location":"api/#pyth_hermes.HermesClient.get_price_decimal","title":"<code>get_price_decimal(feed_id)</code>","text":"<p>Return the latest human-readable spot price for one feed id.</p> <p>Convenience wrapper over :meth:<code>get_latest_price</code> that returns just the spot price as a :class:<code>~decimal.Decimal</code>.</p> <p>Parameters:</p> Name Type Description Default <code>feed_id</code> <code>str</code> <p>A single 32-byte hex feed id.</p> required <p>Returns:</p> Type Description <code>Decimal</code> <p>The latest spot price as an exact <code>Decimal</code>.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the API returns no parsed price for <code>feed_id</code>.</p> <code>HTTPStatusError</code> <p>If the API returns a non-2xx status after retries are exhausted.</p>"},{"location":"api/#pyth_hermes.BinaryUpdate","title":"<code>BinaryUpdate</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>The encoded binary VAA/update payload.</p> <p>This is the on-chain-submittable blob; <code>data</code> is a list of encoded strings whose representation is given by <code>encoding</code>.</p>"},{"location":"api/#pyth_hermes.FeedAttributes","title":"<code>FeedAttributes</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>Attributes block of a price feed catalog entry.</p> <p>The Hermes <code>attributes</code> object is an open string map; the well-known keys are surfaced as typed fields and any extra keys are preserved (accessible via :attr:<code>pydantic.BaseModel.model_extra</code>).</p>"},{"location":"api/#pyth_hermes.ParsedPriceUpdate","title":"<code>ParsedPriceUpdate</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>A single parsed price update for one feed id.</p> <p>Carries both the spot <code>price</code> and the exponentially-weighted moving average <code>ema_price</code>. Call :meth:<code>to_decimal</code> for the human-readable spot price.</p>"},{"location":"api/#pyth_hermes.ParsedPriceUpdate.to_decimal","title":"<code>to_decimal()</code>","text":"<p>Return the human-readable spot price as a :class:<code>~decimal.Decimal</code>.</p> <p>Shortcut for <code>self.price.to_decimal()</code>.</p>"},{"location":"api/#pyth_hermes.PriceFeed","title":"<code>PriceFeed</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>A single price feed catalog entry from <code>GET /v2/price_feeds</code>.</p>"},{"location":"api/#pyth_hermes.PriceFeed.symbol","title":"<code>symbol</code>  <code>property</code>","text":"<p>Shortcut to <code>attributes.symbol</code> (e.g. <code>\"Crypto.BTC/USD\"</code>).</p> <p>Returns:</p> Type Description <code>Optional[str]</code> <p>The canonical symbol, or <code>None</code> if absent.</p>"},{"location":"api/#pyth_hermes.PriceFeedMetadata","title":"<code>PriceFeedMetadata</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>Metadata attached to a parsed price update.</p> <p>All fields are optional; Hermes may omit any of them depending on the endpoint and whether proof data is available.</p>"},{"location":"api/#pyth_hermes.PriceUpdateResponse","title":"<code>PriceUpdateResponse</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>Response from the latest / historical price update endpoints.</p> <p>Returned by <code>GET /v2/updates/price/latest</code> and <code>GET /v2/updates/price/{publish_time}</code>, and by each SSE stream event. <code>parsed</code> is <code>None</code> when the request was made with <code>parsed=False</code>.</p>"},{"location":"api/#pyth_hermes.RpcPrice","title":"<code>RpcPrice</code>","text":"<p>               Bases: <code>BaseModel</code></p> <p>A single price reading with its exponent and publish time.</p> <p>Used for both the spot <code>price</code> and the <code>ema_price</code> of a parsed update. The <code>price</code>/<code>conf</code> fields are raw integers; apply <code>expo</code> (or call :meth:<code>to_decimal</code> / :meth:<code>conf_to_decimal</code>) to get human-readable values.</p>"},{"location":"api/#pyth_hermes.RpcPrice.to_decimal","title":"<code>to_decimal()</code>","text":"<p>Return the human-readable price as a :class:<code>~decimal.Decimal</code>.</p> <p>Returns:</p> Type Description <code>Decimal</code> <p><code>price * 10 ** expo</code> as an exact <code>Decimal</code>.</p>"},{"location":"api/#pyth_hermes.RpcPrice.conf_to_decimal","title":"<code>conf_to_decimal()</code>","text":"<p>Return the confidence interval as a :class:<code>~decimal.Decimal</code>.</p> <p>Uses the same exponent as the price.</p> <p>Returns:</p> Type Description <code>Decimal</code> <p><code>conf * 10 ** expo</code> as an exact <code>Decimal</code>.</p>"},{"location":"api/#pyth_hermes.price_to_decimal","title":"<code>price_to_decimal(price, expo)</code>","text":"<p>Convert a raw Pyth price + exponent into a real <code>Decimal</code> value.</p> <p>Pyth reports prices as a large integer plus a (usually negative) base-10 exponent. The human-readable value is <code>price * 10 ** expo</code>, computed here with :class:<code>~decimal.Decimal</code> to avoid binary float imprecision.</p> <p>Parameters:</p> Name Type Description Default <code>price</code> <code>Union[int, str]</code> <p>The raw integer price, as an <code>int</code> or its string form (Hermes returns it as a JSON string).</p> required <code>expo</code> <code>int</code> <p>The base-10 exponent to apply (e.g. <code>-8</code>).</p> required <p>Returns:</p> Type Description <code>Decimal</code> <p>The exact human-readable price as a :class:<code>~decimal.Decimal</code>.</p> Example <p>price_to_decimal(6395282153102, -8) Decimal('63952.82153102')</p>"}]}